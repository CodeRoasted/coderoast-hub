# 16_late_emerging_benign
# Contract — what it asserts: True-negative: the benign late pattern is not flagged.
#
# A LogCraft scenario / declarative contract — see insight-playground/HOW_TO_READ.md.

meta:
  scenario: scenario/16_late_emerging_benign.yaml
  target_seconds: 30
  axis: time
  windows:
    mode: single
  pyramid_config:
    raw_ring_capacity: 13
    ladder: { levels: 1 }
    raw_strides: []
  metalog_config:
    top_k_size: 2
    reservoir_size: 8
  explain_min_confidence: 0.75
  criticite:
    kind: invariant
  seeds: [default]

signal:
  must_fire:
    - coordinate: RAW.now . Rsv . salience
      emits: NoveltyNGram
      scope: { template: "cache warmer populated" }

interpretation:
  must_not_emit:
    - { any: true }

